"""Message handlers."""
# Currently no message handlers registered
# Add message handlers here if needed in the future


def register_message_handlers(application) -> None:
    """Register all message handlers with the application."""
    # No message handlers currently registered
    pass

